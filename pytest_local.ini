[pytest]
# Configuration for local development - excludes slow tests
addopts = 
    --ignore=tests/mokka/test_adaptive_equalizers.py 
    -m "not slow"
    -v --durations=10 -x

# Mark slow tests for reference
markers =
    slow: marks tests as slow (deselect with '-m not slow')